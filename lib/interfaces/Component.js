"use strict";

const EventEmitter = require('events').EventEmitter;

/**
 * Represents a object that changes the behavior
 * A component adds functionality to the bot.
 * @class
 */
class Component extends EventEmitter {
    constructor() {//TODO

    }
}

module.exports = Component;
